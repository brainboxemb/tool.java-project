name: Reusable Java build publication

on:
  workflow_call:
    inputs:
      publication-artifact-name:
        description: Actions artifact containing the prepared generated build-output tree
        required: true
        type: string

permissions:
  contents: write

jobs:
  publish:
    permissions:
      contents: write
    # Generic branch mapping/push belongs to tool.git-project; Java owns the
    # prepared build tree and the `bld` publication suffix.
    uses: brainboxemb/tool.git-project/.github/workflows/reusable-generated-output-publish.yml@v0.1.3
    with:
      artifact_name: ${{ inputs.publication-artifact-name }}
      branch_suffix: bld
