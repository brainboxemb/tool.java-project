# Java build output

Generated by `tool.java-project` from the canonical Maven verify action.

- Repository: `brainboxemb/tool.java-project`
- Producer source revision: `44fb270c23edd85bdb5e294ad21e76bde36cc0e6`
- Canonical checked-out SHA: `44fb270c23edd85bdb5e294ad21e76bde36cc0e6`
- tool.java-project: `0.3.0` (`44fb270c23edd85bdb5e294ad21e76bde36cc0e6`)
- Java baseline: `8.0.504+1`
- Maven baseline: `3.9.16`
- Maven Wrapper baseline: `3.3.4`

## Artifacts

- `artifacts/minimal-java-app-0.1.0-SNAPSHOT.jar`

## Producer execution evidence

This records the Java producer execution that actually created the retained output.
It stays unchanged when an equivalent result is later hydrated from cache.

- [Machine-readable canonical execution](evidence/executions/java-canonical/execution.json) — capability, producer source revision, exact owner revision and result.
- [Canonical execution log](evidence/executions/java-canonical/execution.log) — human-readable Java/Maven log from that producer run.

## Domain evidence

These files answer Java-specific build and test questions; they are richer domain evidence, not alternate producer logs.

- `evidence/toolchain-build-provenance.txt` — configured/runtime Java, Maven, Maven Wrapper and owner-tool provenance.
- [Readable Surefire summary](evidence/tests/README.md) — aggregate test result with retained raw Surefire reports below `evidence/tests/`.

## Orchestration/materialization evidence

When a consumer runs this output through Moon, the publication layer may add:

- `orchestration/materialization.json` — the current source revision/context for which Moon executed or hydrated this output.
- `orchestration/moon.log` — the human-readable execute/cache/hydrate decision for the current invocation.

After cache hydration, the producer source revision above may intentionally differ from the current materialization source revision.
That difference means the producer did not run again; it is not stale or conflicting provenance.

## Publication context

This tree is generated build output. Publication/finalization is a separate side effect that consumes prepared output and must not rewrite producer evidence.
